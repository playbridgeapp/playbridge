## 2026-03-31 - [Explicit semantics on IconButton] **Learning:**[While standard Jetpack Compose automatically merges child semantics, the strict "Good UX Code" pattern for this repository explicitly requires moving `contentDescription` from the inner `Icon` (set to `null`) to the `IconButton`'s `Modifier.semantics` alongside an explicit `Role.Button`.] **Action:** [Always apply `Modifier.semantics { contentDescription = "..."; role = Role.Button }` directly on `IconButton` instead of relying on default implicit descriptions on inner Icons.]
