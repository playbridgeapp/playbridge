# phone/app/libs

Phone-specific local artifacts (if any).

**FFmpeg decoder AAR** lives at the repo root so phone and TV share one copy:

```
prebuilt/media3/lib-decoder-ffmpeg-release.aar
```

See `prebuilt/media3/README.md` and `app/build.gradle.kts`.
