name: playbridge_cast_core
description: Reusable Dart FFI bindings for PlayBridge Cast Core discovery and sessions.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
