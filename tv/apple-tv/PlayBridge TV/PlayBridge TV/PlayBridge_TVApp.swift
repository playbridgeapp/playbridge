import SwiftUI

@main
struct PlayBridge_TVApp: App {
    var body: some Scene {
        WindowGroup {
            VStack {
                Text("PlayBridge TV — KMP shell wiring in progress.")
                    .font(.largeTitle)
                    .padding()
            }
        }
    }
}
